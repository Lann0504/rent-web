{
  "name": "rent-app-monorepo",
  "private": true,
  "package-Manager": "pnpm@10",
  "scripts": {
    "dev": "pnpm -r dev",
    "dev:web": "pnpm --filter web dev",
    "dev:api": "pnpm --filter api dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  }
}
